#include <cstdlib>
//#include <functional>
#include "gtest/gtest.h"


/** We don't want to test std::tuple_cat() here,
 * but this a demonstration that std::ref() works with std::tuple_cat().
TEST(TupleCatText, StdRef) {
  // TODO: EXPECT_EQ("hello", std::get<0>(t_both));
}
*/

